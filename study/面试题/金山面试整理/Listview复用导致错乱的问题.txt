像textview这些的话即使没数据也要做个setText为空
像图片的话最好就设置个tag然后判断一下是否需要显示

Listview复用的是整个显示在屏幕上的view 比如一屏幕显示10个 那么会生成10个view。（即使你判断了contentView不为空，猜测是内部自己创建后传入一个空）
然后当下滑的时候，就会开始对应去复用，比如第11个会复用第1个。


item里面要指定高度的话要使用minHeight