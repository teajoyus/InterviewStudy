内部是一个LinkHashMap
LinkHashMap是采用双向链表的方式
里面有个accessOrder如果true的话
可以是新插入的结点会放到链表结尾，而且只要更新或者访问这个结点数据的话就会移动到链表结尾
这个特性也就是LruChche的模型了，使得新插入的、更新、或者访问的结点会自动到链表结尾去
那么我们就可以得到一个最近最少使用的结点了。
当用在LruCache的时候如果添加进来的数据空间低于配置的可用值，则会去除掉这个最近最少使用的结点，以确保可以添加新的数据进来